#include <stdio.h>
#include <stdlib.h>
#include "lists.h"
/**
 * check_cycle - function that checks if the list has a cycle
 * @list: list to check
 * Return: 0 or 1
 */
int check_cycle(listint_t *list)
{
	return (0);
}
